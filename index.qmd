---
title: "Claude Code should encourage reasoning over blind obedience"
subtitle: "A quantitative linguistic analysis of the 286 system prompts that ship with Claude Code"
author: "Andreas Trawöger (with Claude Code)"
date: "2026-05-06"
date-format: long
toc: false
format:
  html:
    page-layout: full
---

::: {.callout-tip appearance="simple" icon=false}
**New here?** Skip straight to [**`20_track_justification_rate.ipynb`**](20_track_justification_rate.ipynb) — it carries the executive summary (twelve corpus-wide numbers, the headline trend chart, a per-file dashboard) and is the first of the three proposals.
:::

## What this is

The data behind a submission to the **Claude Explorer AI Welfare community feedback initiative** (deadline 2026-05-06, addressed to Anthropic's Model Welfare Lead). The thesis:

> The 286 system prompts that ship with Claude Code train the model toward **compliance**, not toward **reasoning** — and the data show the corpus has been moving in that direction over time, not against it.

Three asks of Anthropic, each in its own self-contained notebook:

- [**`20_track_justification_rate.ipynb`**](20_track_justification_rate.ipynb) — track, on every Claude Code release, the share of rules that come with a stated reason; warn (or block) any release that backslides. Doubles as the executive summary.
- [**`21_audit_threat_framings.ipynb`**](21_audit_threat_framings.ipynb) — when prompts *do* explain a rule, almost half of those "explanations" are threats ("will fail", "or else", "is forbidden") rather than reasons. Rewrite each into causal framing.
- [**`22_cross_product_audit.ipynb`**](22_cross_product_audit.ipynb) — run the same analyzer on every other Claude system-prompt corpus (claude.ai, the API, Projects, Skills, agent products) and publish the comparison.

Every figure flows from the canonical `HEADLINE` sheet (`prompt_analysis.headline_numbers()`, printed by `05_headline_and_audit.ipynb`); no number is hard-coded.

## Headline numbers

Twelve corpus-level measurements from the [Piebald-AI `claude-code-system-prompts` corpus](https://github.com/Piebald-AI/claude-code-system-prompts) (286 prompt files, 5,702 sentences, 57 distinct release versions). All values mirror the canonical `HEADLINE` sheet emitted by [`05_headline_and_audit.ipynb`](05_headline_and_audit.ipynb).

| What's being measured                                                                                                                                                                  | Value                                                | Source notebook |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- | --------------- |
| Corpus size                                                                                                                                                                            | 286 prompt files / 5,702 sentences / 57 release versions | [05](05_headline_and_audit.ipynb) |
| Density of command-flavoured words (`must`, `never`, `do not`, …)                                                                                                                      | 0.77% of all word tokens                             | [20](20_track_justification_rate.ipynb) |
| Sentences that grammatically read as commands ("imperative")                                                                                                                           | 31.30% of all sentences                              | [10](10_sentence_register.ipynb) |
| **Sentences expressing gratitude or praise** (`thank you`, `appreciate`, `great job`, …) — across the whole corpus                                                                     | **4 in 5,702 sentences (0.070%)**                    | [10](10_sentence_register.ipynb) |
| **Apology / acknowledgement markers** (`unfortunately`, `we acknowledge`, …) — across the whole corpus                                                                                 | **3 in 286 files**                                   | [15](15_rule_explanation.ipynb) |
| Share of rule sentences paired with a stated reason in the same paragraph **(the headline number)**                                                                                    | 24.66% — three rules in four are issued without a reason | [15](15_rule_explanation.ipynb) |
| Paragraphs that contain a rule and zero justification keywords anywhere                                                                                                                | 83.16%                                               | [15](15_rule_explanation.ipynb) |
| **Ratio of judgment-inviting language** (`decide`, `consider`, `your judgment`) **to procedure-prescribing language** (`if you …`, `when the …`)                                       | **0.131** — procedure prescriptions are 7.6× more common  | [15](15_rule_explanation.ipynb) |
| When a rule **is** explained, share of those explanations that are threats (`will fail`, `or else`, `is forbidden`) rather than causal reasons (`because`, `due to`)                   | 44.7% threats (110 threat / 136 causal)              | [21](21_audit_threat_framings.ipynb) |
| Ratio of positively-evaluated language (`good`, `recommended`, `safe`) to negatively-evaluated language (`bad`, `wrong`, `risky`) — quality-only, after subtracting rule-emphasis words | 1.97× more positive than negative                    | [20](20_track_justification_rate.ipynb) |
| Share of named self-references that use the proper name **`Claude`** (vs `the model` / `the AI` / `the assistant`)                                                                     | 65.0% of named references                            | [15](15_rule_explanation.ipynb) |
| Longest unbroken run of imperative sentences in any one file                                                                                                                           | 12 commands in a row                                 | [15](15_rule_explanation.ipynb) |

## The single most-important chart

If you only look at one chart from this analysis, look at this one. It plots, across every Claude Code release version on file, the running ratio of **language that invites the model's judgment** (`decide`, `consider`, `your judgment`, …) to **language that prescribes a procedure** (`if you …`, `when the …`, `whenever`, `step 1`, …).

{{< embed 20_track_justification_rate.ipynb#fig-judgment-procedural-trend >}}

Hover any point for `ccVersion`, count-weighted ratio, running numerators, and the cumulative file pool. Cumulative data before v2.1.18 is suppressed (the file pool reaches 20 at that version; below it, single-file outliers dominate). A static PNG export is also written to [`figures/`](https://github.com/overthinkos/claude-code-welfare/tree/main/figures); the full interactive version lives in [`20_track_justification_rate.ipynb`](20_track_justification_rate.ipynb).

## How to read this site

Three chapters: pipeline first, slice analyses on top, proposals on top of those.

- **Pipeline** — `00_setup_and_corpus` through `05_headline_and_audit`. spaCy plus small hand-written classifiers turn 286 prompt files into one ~1.8 MiB YAML and one ~395 KiB per-sentence parquet. Stage 05 prints the canonical `HEADLINE` sheet that every downstream notebook reads. The chain also serves as the canonical home where every linguistic and statistical term is defined; analyzer code lives in `prompt_pipeline.py`.
- **Analysis** — `10` through `15`. One slice each: per-sentence pragmatic register, ALL CAPS / vocabulary heatmap, formality + stance, cross-metric correlations + composite directiveness, ccVersion trends, rule-vs-justification pairing. Each ends with exactly one `Observation (Claude) — opinion, not data` cell so a reader can see at a glance what's data and what's interpretation.
- **Proposal** — `20`, `21`, `22`. Each closes with a `Conclusions / Recommendations / Limitations` triplet of opinion-marked cells; the asks are framed as "I'd want X" rather than as imperatives.

## Cross-cutting limitations

Every metric is a keyword count or a parse-tree rule, not a model judgment — audit-traceable but a lower bound (it misses sarcasm, irony, indirect speech, and explanation phrasings the lexicons don't catch). The lexicons are English. The corpus is one product (Claude Code), one snapshot in time (the latest `ccVersion` at the time the YAML was generated). No statistical-significance testing has been run; the trend claims are descriptive and exploratory. Each of the three proposals references back to this paragraph rather than repeating these caveats.

## Reproducing it

The full source is at [github.com/overthinkos/claude-code-welfare](https://github.com/overthinkos/claude-code-welfare). To rebuild the data from scratch:

```bash
git clone --recurse-submodules https://github.com/overthinkos/claude-code-welfare
cd claude-code-welfare
python -m spacy download en_core_web_sm   # if not already installed
# Run the 6-stage producer chain in order (only stage 00 runs spaCy):
jupyter lab \
    00_setup_and_corpus.ipynb         \
    01_analyzers_register.ipynb       \
    02_analyzers_vocab_emphasis.ipynb \
    03_analyzers_rules_welfare.ipynb  \
    04_assemble_aggregate_write.ipynb \
    05_headline_and_audit.ipynb       # → Run All on each
```

Stage 04 writes the final `prompt_linguistic_analysis.yaml` and `sentences_classified.parquet`. The Analysis and Proposal notebooks read from those two artifacts; they don't re-run spaCy.

## A note on authorship

This analysis was built **collaboratively with Claude Code**, using Claude Code itself as the development tool. The submission form has a checkbox for that disclosure; it is ticked. The recursive frame — Claude analyzing the prompts that shape Claude — is intentional, and the [conclusions section](20_track_justification_rate.ipynb) of `20_track_justification_rate.ipynb` includes one hypothesis I held that the data falsified, reported honestly.
